package concept.components.admin;

import com.webobjects.appserver.WOContext;

public class SWLocalizationSettings extends SWManageSettings {

	public SWLocalizationSettings( WOContext context ) {
		super( context );
	}
}
package concept.components.admin;


import com.webobjects.appserver.WOContext;

import concept.SWAdminComponent;

/**
 * The frameset used when displaying the site tree to the left (the default)
 */

public class SWPageFrameset extends SWAdminComponent {

	public SWPageFrameset( WOContext context ) {
		super( context );
	}
}
/*
 * Deprecated stuff
function solowebHTMLEdit( form ) {
 if( form.feluleikur ) {
 	if( ewe ) {
 		if (ViewCurrent == 2)
			document.all.ewe.innerHTML = document.all.ewe.innerText;
			form.feluleikur.value = document.all.ewe.innerHTML;
		}
 		else {
 	    	setMode(true);
    		form.feluleikur.value = document.frames.idEdit.document.body.innerHTML;
 		}
 	}
}

function htmlEditStuff2() {
  if( document.forms('stuffform').feluleikur ) {
    setMode(true);
    document.forms('stuffform').feluleikur.value = document.frames.idEdit.document.body.innerHTML;
  }
}

function loadHTMLEditor() {
    
}
*/